services:
  app:
    build: .
    restart: unless-stopped
    # ports:
    #   - "3000:3000"
    environment:
      VIRTUAL_HOST: "lars-rickert.de"
      LETSENCRYPT_HOST: "lars-rickert.de"

networks:
  default:
    name: nginx-proxy
    external: true
